/* import styled from 'styled-components';

const MinMaxFilterStyled = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  overflow-y: scroll;
  overflow-x: hidden;
  height: ${({ height }) =>
    height === 'lg' ? '300px' : height === 'sm' ? '100px' : '200px'};
  width: ${({ width }) =>
    width === 'lg' ? '500px' : width === 'sm' ? '250px' : '350px'};
  padding: 10px;
  border: 1px solid black;
`;
const MinMaxFilter = ({ width, height, action, checked }) => {
  return <MinMaxFilterStyled placeholder={placeholder} type={type}></MinMaxFilterStyled>;
};

export default MinMaxFilter;
 */
